models:
  - name: test_id_stitcher
    model_type: id_stitcher
    model_spec:
      entity_key: user
      validity_time: 24h # 1 day
      main_id_type: main_id
      exclude_id_types:
        - exclude_id
      edge_sources:
        - input: tbl_a
        - input: tbl_b
        - input: tbl_c
